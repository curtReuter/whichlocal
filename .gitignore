node_modules/
dist/
.env
# local dev overrides only; js/config.js is the committed default
js/config.local.js
.DS_Store
*.log
.claude

# PocketBase — binary and local database are not committed
pb/pocketbase
pb/pb_data/
pb/CHANGELOG.md
pb/LICENSE.md

# Scraper — cache the raw HTML locally, but keep geocache.json tracked
scripts/cache/*.html
# per-run "what's new" delta consumed by notify-discord.mjs
scripts/cache/job-calls-delta.json
# raw OLMS filer dump — 20+ MB, only used for scrape-ibew-roster.mjs --offline
scripts/cache/olms-filers.json
