{
  "__doc__": "Locals whose own site publishes a job-calls / referral list, keyed by slug (the `id` in the ranked list). Add an entry to scrape another local; `scripts/scrape-job-calls.mjs` parses each `url` into js/data/job-calls.json. Spot-check a new one with `node scripts/scrape-job-calls.mjs --only <slug>`. The `discord` block routes notifications: one forum channel per state, one thread per local (created on first post; ids kept in scripts/discord-threads.json). Channel ids aren't secret — the bot token is (repo secret DISCORD_BOT_TOKEN).",
  "discord": {
    "forums": {
      "FL": ""
    },
    "default_channel_id": ""
  },
  "l606-orlando-fl": {
    "local_no": 606,
    "url": "https://www.ibew606.org/?zone=/unionactive/view_page.cfm&page=Job20Calls"
  },
  "l756-daytona-beach-fl": {
    "local_no": 756,
    "url": "https://www.ibew756.org/?zone=/unionactive/view_page.cfm&page=Job20Calls"
  }
}